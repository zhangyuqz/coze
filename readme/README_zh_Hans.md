# 扣子版思维导图：本次修复与实测结果

2026 年 9 月 22 日，已登录你的扣子账号，在现有 `generate_mind_map` 工具里完成实际云端测试。修复代码和新的运行包地址已经填入，不需要你再重复粘贴。

## 原来为什么一直运行、最后没有图片

这次在扣子中复现的是：程序下载 GitHub 上的运行包时超时，尚未开始绘图。不是测试 Markdown 的格式问题，也没有证据表明是这次输入触发了内存不足。

现在将原运行包拆成 8 片，通过 jsDelivr 下载，再按原顺序还原。每片及完整文件都核对 SHA-256。还原后的文件与原运行包完全相同，绘图代码、字体和排版算法没有修改。只有下载这些静态文件时采用并行，不是同时启动 8 次绘图。

这个方案仍需联网；外部网络完全不可用时，首次运行仍可能失败，不能保证任何时候都不会超时。

## 实际生成了什么

| 测试输入 | 字体 | 输出 | 实际尺寸 | 文件大小 |
| --- | --- | --- | --- | ---: |
| 世界地图多语种小样 | 思源黑体 | 深色 PNG | 1120×580 | 63,826 字节 |
| 原有 30 语种小样 | Yeseva One | 浅色 WebP | 1910×7223 | 378,814 字节 |
| 原有 30 语种大型压力样例 | Bad Script | 深色 PNG | 2826×19950 | 2,239,987 字节 |
| 世界地图多语种小样 | 站酷快乐体 | 深色 GIF，12 帧 | 1080×564 | 166,527 字节 |

以上四次都完成了：扣子云端绘图 → 上传到扣子文件存储 → 通过原有已发布工作流取得图片地址 → 下载并打开真实文件。不是只看到了页面上的“测试通过”。

已检查 PNG 小样和 GIF 的整体画面，并在浏览器中按原始像素检查长图的代表性区域，包括多语种文字、混合形状、长文本、竖向节点和八级分支。在已检查区域内，没有观察到文字碰到边框、文字超出节点、节点互相重叠或连接线断开。GIF 已实际看到边框颜色变化。

这里的视觉检查不等于所有语言逐字的语言学审校，也不等于所有字体与参数组合已经穷举测试。

## 现在的运行包地址

`runtime_bundle_url` 已设置为下面的完整地址：

```text
https://cdn.jsdelivr.net/gh/zhangyuqz/coze@3911bdaa71ec797929e457bda8c24e7bc004f7ae/runtime/0.0.23-coze.1/manifest.json
```

绘图运行包版本仍为 `0.0.23-coze.1`，因为里面的绘图程序和字体没有改动。本次修改的是扣子入口的下载方式，不是新的 Dify 版本。

## 保持不变的内容

- 原 Dify 插件、原发布包、原 GitHub Release 均未改动。
- 绘图核心、17 款字体、字体排序、图形选择、节点布局和图片清晰度逻辑均未改动。
- 原有文件上传和图片链接解析流程继续使用。
- 没有开通付费服务，没有购买服务器或图床。
- API 令牌没有写入本地交付文件或 GitHub 仓库。

扣子的额度和是否产生平台费用仍以账号规则为准，不能把本次未开通付费服务理解为无限免费。返回的是带签名的图片地址，也不能当作永久不失效的图床地址。

## 保留的测试边界

目前插件仍是未发布状态。本次已验证 IDE 云端运行及文件链路，没有发布到插件市场，也没有宣称普通工作流中的已发布插件节点、十路并发或全部字体组合均已测试。

后续若再失败，请看输出中的 `success`、`error_stage` 和 `error`。扣子页面显示“测试通过”，只说明代码按约定返回了结果；如果 `success` 为 `false`，仍然是生成失败。

参数类型保持：`render_scale`、`max_blob_mb` 为 Number；`success` 为 Boolean；图片宽、高和字节数为 Number。不要把数字字段重新改回 String。

